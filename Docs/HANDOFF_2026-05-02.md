# 2026-05-02 阶段性交接

## 本阶段目标

把项目从“能跑但目录和职责逐渐发散”的状态，先整理到更容易继续维护的状态。本阶段尽量不改业务行为，重点是降低后续继续开发的混乱度。

## 已完成

1. 建立项目入口和目录约定
   - 新增 `VoiceServiceDemo.slnx`，统一包含桌面端、MCP、共享库和自检项目。
   - 新增 `.gitignore`，忽略 `bin/`、`obj/`、日志、临时音频和 `.local/`。
   - 新增 `Docs/PROJECT_STRUCTURE.md`，说明主要目录的职责。

2. 收纳根目录杂物
   - 功能说明移动到 `Docs/features/`。
   - 供应商接入笔记移动到 `Docs/providers/`。
   - 实施计划移动到 `Docs/plans/`。
   - 阿里音色原始数据移动到 `data/aliyun/`。
   - 辅助脚本移动到 `scripts/`。
   - 设计稿移动到 `assets/designs/`。
   - 构建日志和测试音频保留在本机 `.local/`，不再进入 Git。

3. 清理 Git 生成物
   - 已把根目录 `bin/`、`obj/` 生成物从 Git 跟踪中移除。
   - 后续构建仍会在本地生成这些文件，但不会再污染版本库。

4. 拆分火山 TTS
   - 新增 `Services/Providers/HuoshanTtsProvider.cs`。
   - `Services/TtsService.cs` 现在只负责统一入口和分发，火山的连通测试、音色拉取、普通合成、V3 合成、长文本合成都移入 Provider。
   - `TtsService.cs` 已从约一千多行降到约 633 行。

5. 补充自检
   - `VoiceServiceDemo.Tests` 增加火山 Provider 边界测试。
   - 增加运行资源复制测试，确认 `data/aliyun/voices_raw.json` 会复制到运行目录的 `aliyun_voices_raw.json`。

## 已验证

```powershell
dotnet run --project .\VoiceServiceDemo.Tests\VoiceServiceDemo.Tests.csproj
```

结果：火山凭证解析、MCP 火山凭证解析、共享火山协议、火山 Provider 边界、运行资源复制全部通过。

```powershell
dotnet build .\VoiceServiceDemo.slnx
```

结果：解决方案构建成功，0 个警告，0 个错误。

## 当前仍需注意

1. `Services/TtsService.cs` 仍然偏大，后续建议继续按供应商拆分。
2. `VoiceServiceMcp/Core/TtsService.cs` 仍有供应商实现重复，后续应逐步复用 `VoiceServiceShared` 或桌面端 Provider 的共享逻辑。
3. `VoiceServiceDemo.Tests` 目前还是控制台自检项目，不是标准 xUnit/NUnit 测试项目。
4. 工作区中还有其他未提交修改，提交前需要继续按主题分批处理，避免一个提交混入过多不相关内容。

## 下一步建议

优先继续拆 `TtsService.cs`，推荐顺序：

1. 拆 `AliyunTtsProvider`，因为阿里已有本地音色数据和复制路径，边界比较清楚。
2. 拆 `TencentTtsProvider`，同时检查桌面端和 MCP 端签名逻辑是否能共享。
3. 最后处理百度、Azure、Google、OpenAI 这些相对较短的实现。

每拆一个供应商，都保持同样节奏：

1. 先加一个最小自检。
2. 把供应商实现移到 `Services/Providers/`。
3. `TtsService` 只保留分发。
4. 跑自检和解决方案构建。
5. 阶段提交。
